#include <unordered_map>
#include "Cube.hpp"
#include <iterator>

class HashTable {
  
public:
<<<<<<< HEAD
    HashTable();

    void insert(Cube*);
    bool contains(Cube*);
    bool empty();
    int size();

=======
  HashTable();
  ~HashTable();
  void insert(Cube*);
  bool contains(Cube*);
  int size();
  
>>>>>>> master
private:
  std::unordered_multimap<std::string, Cube*> table;
};
